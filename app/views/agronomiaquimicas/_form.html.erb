<%= form_for(@agronomiaquimica) do |f| %>
  <% if @agronomiaquimica.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@agronomiaquimica.errors.count, "error") %> prohibited this agronomiaquimica from being saved:</h2>

      <ul>
      <% @agronomiaquimica.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Fecha_hora %><br />
    <%= f.datetime_select :Fecha_hora %>
  </div>
  <div class="field">
    <%= f.label :WhTot %><br />
    <%= f.number_field :WhTot %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
