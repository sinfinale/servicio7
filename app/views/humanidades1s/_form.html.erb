<%= form_for(@humanidades1) do |f| %>
  <% if @humanidades1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@humanidades1.errors.count, "error") %> prohibited this humanidades1 from being saved:</h2>

      <ul>
      <% @humanidades1.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Fecha_hora %><br />
    <%= f.datetime_select :Fecha_hora %>
  </div>
  <div class="field">
    <%= f.label :WhTot %><br />
    <%= f.number_field :WhTot %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
