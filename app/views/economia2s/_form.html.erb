<%= form_for(@economia2) do |f| %>
  <% if @economia2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@economia2.errors.count, "error") %> prohibited this economia2 from being saved:</h2>

      <ul>
      <% @economia2.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Fecha_hora %><br />
    <%= f.datetime_select :Fecha_hora %>
  </div>
  <div class="field">
    <%= f.label :WhTot %><br />
    <%= f.number_field :WhTot %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
